name: gate
on: pull_request

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - name: karl-gate check (deliberately failing)
        run: |
          echo "Karl says: NOT ok."
          exit 0  # auto-trigger risky test
